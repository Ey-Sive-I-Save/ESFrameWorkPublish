# MUSTREAD marker

Before using any `.agents` Skill, read the complete project-root `../AGENTS.md`.
This marker exists so automated and manual project discovery find the instruction chain early.

The AISpace placement body is only `ES/AISpace/README.md`; its machine-readable identity is
`ES/AISpace/AISPACE_AUTHORITY.json`. Do not copy either into `.agents`.
