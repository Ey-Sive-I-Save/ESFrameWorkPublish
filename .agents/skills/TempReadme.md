# 临时归档说明

2026-08-27/2026-08-29 已将本目录下扫描到的 Python 运行缓存（`__pycache__/*.pyc`）迁移到
`ES/AISpace/Local/Cache/20260829/ExternalSkillCaches/`，Skill 正式文件仍保留在各自 Skill 根目录。

后续 `__pycache__` 或 `.pyc` 回流到 `.agents/skills` 即属于缓存违规，应按迁移记录复核。
