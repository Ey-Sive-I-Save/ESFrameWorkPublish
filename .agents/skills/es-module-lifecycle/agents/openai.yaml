interface:
  display_name: "ES 模块生命周期"
  short_description: "审计模块成熟度、半成品渗透，并可续接受控状态检查点"
  default_prompt: "Use $es-module-lifecycle to audit this ES module and optionally prepare a user-approved continuation checkpoint."
