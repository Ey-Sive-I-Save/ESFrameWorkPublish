interface:
  display_name: "ES 发布验收"
  short_description: "编排编译、测试、Profiler、Player 与资源发布证据"
  default_prompt: "Use $es-release-acceptance to run and report the required ES release acceptance evidence."

dependencies:
  tools:
    - type: "mcp"
      value: "unityMCP"
      description: "Run and inspect acceptance evidence in the active Unity Editor."
      transport: "streamable_http"
      url: "http://127.0.0.1:8080/mcp"
