# JSON reader

Strict UTF-8; parse structure and bounded samples. Reject malformed JSON, traversal in source identity, duplicate object keys when the strict parser is available, and unbounded expansion. Preserve source SHA-256 and parser version.
