# Table reader

CSV/TSV only on the fast path. Return delimiter, headers, row count, inferred scalar types, and bounded head/tail samples. Do not evaluate formulas or macros. XLSX is a separate adapter.
