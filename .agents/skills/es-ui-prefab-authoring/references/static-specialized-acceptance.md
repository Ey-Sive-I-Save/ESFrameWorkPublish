# ScreenSpec v3 static acceptance

Acceptance id: ui-prefab-static

Required static cases: schema-validation, registry-boundary, path-boundary, deterministic-plan, evidence-boundary.

Static acceptance proves schema and registry checks, project boundary declarations, deterministic planning and evidence separation. Runtime Unity materialization, Prefab/Fixture Scene output, GPU screenshots and visual fidelity remain unproven until an authorized runtime receipt exists.
