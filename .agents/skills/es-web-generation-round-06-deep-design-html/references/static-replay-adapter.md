# StaticReplay adapter

Replay the Skill with the manifest cases using project-local JSON fixtures. The adapter must keep design acceptance, artifact writing, and static validation separate; it must never start a browser or Runtime.
