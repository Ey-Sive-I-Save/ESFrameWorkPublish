# Step 03 — Capability analysis

## AI analysis

AI analyses the user outcome and derives core, supporting, data, feedback, exception and recovery capabilities. `Invoke-ESWebOpenSourceCapabilityProfile.ps1` and the compiler are executed; the receipt must be `accepted` with six pinned framework evidence records.

## Execution

Run the profile and compiler scripts using the pinned source manifest; bind each strategy to a requested capability.

## Return

Return capability decisions and strategy hashes; missing framework evidence is blocked.

Required reads: `open-source-source-manifest.json`, `open-source-capability-profile.json`, and the capability coverage matrix. Return analysis, execution command, and receipt hash.
