# Step 05 — Interaction design

## AI analysis

AI defines happy path, secondary path, interruption, recovery, focus and state transitions. The design receipt must enumerate controls and observable feedback; generic prose without a transition is insufficient.

## Execution

Compile the interaction state machine and pass its event/guard/effect definitions to deep design and static materialization.

## Return

Return event, guard, transition, effect, and feedback for every interaction.

Required reads: intent receipt, knowledge decisions, and capability state-machine strategy. Return event, guard, transition, effect, and feedback for every interaction.
