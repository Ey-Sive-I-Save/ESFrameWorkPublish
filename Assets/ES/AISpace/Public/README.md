# ES AISpace Public

这里只放必须被 Unity 导入或引用的 AI 协作公共内容，并按领域建立子目录。
私有内容放在项目外的 `ES/AISpace/Local`；不要创建 `Local` 子目录。

AI 生成内容的总放置规则见 `ES/AISpace/README.md`。
本目录只是 Unity 导入出口，不是第二个 AISpace 根；身份与竞争态规则以
`ES/AISpace/AISPACE_AUTHORITY.json` 为准。
