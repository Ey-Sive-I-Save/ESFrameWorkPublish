# Generated Composite Shader Test Assets

此目录由 `ESCompositeShaderTestAssetsBuilder` 维护。Scene、Material 和 Texture 都是可重建测试产物；布局或配方调整应修改 `../Editor` 下的构建器，再通过 Unity 菜单刷新。

不要把这里的资产注册到 `ESNormalAssets`、GameCore、资源 Library、Manifest 或正式发布清单。
