# TODO（Core/Domain/Module）

> 目的：为后续“脱离编辑器装配（非 Inspector 依赖）”与“更高自由度定制能力”做备忘。
> 当前暂不支持，仅记录需求与方向。

## 待办方向

1. **无编辑器装配的域/模块声明方式**
   - 运行时或配置驱动自动装配。
   - 支持从配置/表/脚本创建模块并注入到域。

2. **统一的“可选模块”策略**
   - Core/Domain 层面对“模块存在与否”的统一查询与降级策略。

3. **模块生命周期扩展点**
   - 为模块提供可配置的生命周期钩子顺序（Before/After Update）。

4. **域内高频逻辑内聚**
   - 明确“超高频/高稳定需求”直接写在 Domain 的推荐模式。

5. **KCC 接入准备**
   - 预留 KCC 控制器的 Domain/Module 接口位（后续加入）。

6. **面向策划配置的模块注册**
   - 允许使用中文名（TypeRegistryItem）进行配置检索与自动绑定。
