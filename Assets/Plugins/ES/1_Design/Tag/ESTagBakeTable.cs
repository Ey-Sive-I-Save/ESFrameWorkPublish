namespace ES
{
    // Unity resolves ScriptableObject assets through a same-named script file. The implementation
    // remains with the other Tag value types in ESTag.cs; this partial declaration is its asset anchor.
    public sealed partial class ESTagBakeTable
    {
    }
}
