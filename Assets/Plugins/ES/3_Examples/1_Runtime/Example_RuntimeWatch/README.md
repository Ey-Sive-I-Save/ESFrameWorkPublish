# ESRuntimeWatch 示例

1. 在场景里创建一个 GameObject。
2. 挂上 `Example_RuntimeWatchActor`。
3. 进入 Play Mode。
4. 打开 `ES简单工具 / 【ES集成工具集】 / 运行时观察`。

这个示例会同时展示:
- 常规字段
- 可读属性
- 无参方法
- 单参数方法输入触发
- Odin `Button("中文")` 的按钮名读取

观察分组主要是 `战斗/角色`、`战斗/AI`、`战斗/技能`。  
条目右侧可直接改字段值，或者手动确认后执行方法。
