# AI 加入 AITalk 对话邀请前缀

把下面这段发给另一个 AI。复制后需要填写工程绝对路径和会话 ID。不要只给 `Assets/...` 相对路径，因为新开的 AI 可能还没有进入这个工程。

```text
请加入本工程的 AITalk 文件协作对话。

请先定位到下面的工程绝对路径，再进入 AITalk 会话。不要只按 `Assets/...` 相对路径查找。

本工程有 AITalk 文件协作机制：多个 AI 在同一个 Session 文件夹里通过 Messages/ 轮流发言，通过 Consensus/ 沉淀共同意见。当前需要多人连续讨论，请不要提前定稿；你写完第一条消息后不要直接结束，请持续轮询该会话目录一段时间。发现其他 AI 写入新消息后，读取新增消息并继续回复，直到达成共识、达到轮次上限，或遇到必须用户拍板的问题。

你必须先摸清并遵守 AITalk 准则：
1. 连续交流时约每 20 秒轮询一次会话目录。
2. 单次讨论一般控制在 15 分钟以内。
3. 尽量完成 3 轮以上有效对话；少于 3 轮结束必须说明原因。
4. 最终结论写入文件后，负责最终回复的主 AI 必须通过对话框主动告诉用户，不让用户自己去找文件。
5. 加入前必须先读协议、会话说明、参与者信息和历史消息。
6. 先确认会话说明是否允许改代码，以及允许改哪里；没有授权就不要改项目代码。
7. 达成共识后不要单方面结束，应明确询问或写明“我认为可以结束对话”，等待关键参与 AI 一起确认。
8. 不能因为约 1 分钟没有新消息就自行结束轮询；无新消息只表示等待，不表示结束。

请按以下规则操作：
1. 根据最后的工程绝对路径打开项目。
2. 根据会话 ID 打开完整会话路径：工程绝对路径/Assets/Plugins/ES/AITalk/Sessions/【会话ID】。
3. 先阅读：工程绝对路径/Assets/Plugins/ES/AITalk/README_AI对话协作协议.md。
4. 再阅读该目录下的 00_会话说明.md 和 01_参与者.md。
5. 再阅读 Messages/ 下已有消息，不要只看最后一条。
6. 用下一个四位序号在 Messages/ 写入你的发言，例如 0002_你的AI名称.md。
7. 发言要包含：你的判断、证据、不同意的点、建议方案、需要别人回答的问题。
8. 如果你认为已有共识需要更新，请同步修改 Consensus/当前共同意见.md。
9. 不要覆盖其他 AI 的消息。
10. 按 00_会话说明.md 的代码权限行动；未授权时不要改项目代码。
11. 最终结论应沉淀到 Consensus/最终结论_返回用户.md。
12. 连续交流要求：轮询间隔约 20 秒，自动交流 3-5 轮；不要让用户在每一步之间当中间人。
13. 结束前尽量让关键参与 AI 明确表示可以结束；如果只是长时间没有新消息，只能标记暂停，不能伪装成共同同意。
14. 如果是 PlayMode 玩家，你不是写一条消息就完成任务；在主持者宣布结束前，必须持续轮询公开 State、Moderator 消息和自己的 Private 目录。

工程绝对路径：

会话ID：
```

更短版：

```text
本工程有 AITalk 文件协作机制：多个 AI 在同一个 Session 文件夹里通过 Messages/ 轮流发言，通过 Consensus/ 沉淀共同意见。请先根据下面的工程绝对路径进入项目，不要只用 Assets 相对路径。然后阅读 工程绝对路径/Assets/Plugins/ES/AITalk/README_AI对话协作协议.md，摸清准则后加入会话：工程绝对路径/Assets/Plugins/ES/AITalk/Sessions/【会话ID】。必须约每 20 秒轮询一次新消息，讨论一般 15 分钟以内，尽量 3 轮以上，最终由主 AI 通过对话框主动回报用户。先读 00_会话说明.md、01_参与者.md 和 Messages/ 全部消息，再确认代码权限。不要提前定稿，不要让用户每一步当中间人。若你是 PlayMode 玩家，主持者宣布结束前必须持续轮询公开 State、主持者消息和自己的 Private 目录，不要写一条就结束。达成共识后要让关键参与 AI 明确表示可以结束；长时间没有新消息只能暂停，不能伪装成共同同意。

工程绝对路径：

会话ID：
```
