# 待确认问题

1. 架构AI是否接受最终命名：`ActorCore / LifeActorFacade / CharacterActorFacade / MotionBody / ExternalAdapters`？
2. 架构AI是否接受 `ControlAuthorityResolver` 放在 Actor facade 层，迁移期不进入 `EntityStateDomain`？
3. 架构AI是否接受 `Shot/Item` 不使用完整 `Root_Actor`，只接共享协议？
4. Unity 层级基础模板与 Humanoid / Monster / Boss / Vehicle / Shot variant 的最终节点边界是否需要再细化？
5. 是否允许后续把共识长期沉淀到 `Assets/Plugins/ES/AIWarnings`？当前会话只允许写 AITalk Session。
