# AITalk 会话说明

会话ID：

主题：

用户目标：

当前工程路径：

AITalk绝对路径：

Session绝对路径：

等待其他 AI 加入：是/否

必需参与者：

已到齐：是/否

缺席参与者：

连续交流模式：是

轮询间隔：约 20 秒

建议讨论时长：15 分钟以内

建议有效轮次：至少 3 轮，通常 3-5 轮

提前定稿：禁止

是否允许改代码：是/否

允许改代码范围：

```text
仅会话文件夹 / 指定项目目录 / 禁止改代码
```

最终主 AI：

最终回报方式：

```text
主 AI 必须通过对话框主动告诉用户，不要求用户自己打开文件查看。
```

结束确认要求：

```text
达成共识后，关键参与 AI 应明确写出“我认为可以结束对话/可以进入最终结论”。
如果只是长时间没有新消息，只能暂停，不能伪装成共同同意结束。
```

到齐检查要求：

```text
如果必需参与者未到齐，主 AI 必须继续轮询，并通过对话框告诉用户还缺谁。
未到齐前不能正式开始讨论、游戏、投票、结论定稿或代码执行。
不要因为暂时没人来就终止会话。
```

参与规则：

```text
1. 先阅读 Assets/Plugins/ES/AITalk/README_AI对话协作协议.md。
2. 邀请其他 AI 时必须提供当前工程路径和 Session 绝对路径，不要只给 Assets 相对路径。
3. 再阅读本文件、01_参与者.md 和 Messages/ 下全部消息。
4. 不要只看最后一条消息。
5. 每个 AI 使用下一个四位序号写入 Messages/。
6. 每轮发言后必要时更新 Consensus/当前共同意见.md。
7. 按本文件的代码权限行动。
```
