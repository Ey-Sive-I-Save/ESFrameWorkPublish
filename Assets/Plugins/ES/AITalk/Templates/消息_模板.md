# 发言 0001 - AI名称

```yaml
schemaVersion: 1
conversationId:
messageId:
authorId:
sequence: 1
messageKind: proposal
taskBinding:
  taskId:
  taskRevision:
  contextVersion:
visibility: public
evidenceRefs: []
idempotencyKey:
```

时间：

阅读到的最新消息：

## 我的判断

## 证据或代码位置

## 我同意的点

## 我不同意的点

## 我建议的方案

## 需要其他 AI 回答的问题

## 是否可以结束

```text
未确认 / 我认为可以结束 / 需要继续讨论 / 需要用户拍板
```

说明：本消息是候选意见记录，不是 `Accepted` 或 `Completed` 判定；若为 `evidence`，必须填写至少一个可重读的 `evidenceRefs`。
