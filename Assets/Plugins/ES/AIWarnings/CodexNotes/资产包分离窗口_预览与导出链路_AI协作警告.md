# 资产包分离窗口_预览与导出链路_AI协作警告

记录日期：2026-07-21

负责范围：`ES资产包分离窗口`、资产预览、小格子动画帧缓存、资源分类排序、导出链路与回退。

## 模块定位

- 这是编辑器资源治理工具，不是运行时资源加载系统。
- 核心链路是：资源包烘焙 -> 分类/搜索/排序 -> 预览 -> 标记使用 -> 导出前依赖通报 -> 分类目录复制 -> 链路记录 -> 回退。
- 运行时系统如 `ESInput`、`ESCommand`、`GameManager`、`RuntimeMode`、`Interaction` 不应直接依赖本窗口。
- 与 `State` 的关系仅限编辑器动画预览：可读取 `StateMachineConfig.previewModel / previewAvatar / previewIdleClip` 作为预览配置，不代表运行时状态机架构。

## 当前有效结论

- 小格子动画预览使用“生成帧 + 磁盘缓存”路线。
- 大预览窗口使用实时渲染，保留旋转视角、调试信息和高质量观察能力。
- 预览工作流入口已集中到 `ESAssetPackagePreviewWorkflow`：生命周期注册、ReloadDomain 清理、刷新静态预览缓存、清小格子内存帧、读取保护状态、解析预览模型/Avatar/兜底材质都应从这里走。
- 小格子队列必须当前页优先；切页、切视角、筛选、排序、分类变化后，新页面任务应排到队列最前。
- 小格子缓存不放在 `Assets/`，当前设计放在项目根目录外部缓存文件夹，避免 AssetDatabase 导入污染。
- 导出复制目标使用 `ES选用_` 前缀，按类型自动分目录。
- `ES选用_` 是默认值，不是硬编码死规则；每个 `ESAssetPackageBakeData` 可单独配置导出文件名前缀，窗口配置区也应暴露这个字段。
- 重复导出默认不覆盖、不生成 `_1`，依赖源 GUID -> 目标路径链路判断，避免目标目录资源污染。
- 导出后记录 `exportLinks` 列表和 `exportChainBySourceGuid` 字典；字典依赖 Odin/ESSO 序列化。
- 导出后，如果源资源有有效链路且目标仍存在，应显示为已选中/已导出；链路目标被删除则不再视为有效导出。

## 过时理解，禁止继续传播

- [过时] “小格子每个动画都实时 PreviewRenderUtility 播放”
  - 实测不稳定且性能差，已改为缓存帧。

- [过时] “AnimatorController 临时生成用于预览”
  - 该方案引发 `UnityEditor.Graphs.Edge.WakeUp` 等异常，已废弃。

- [过时] “重复导出自动生成唯一文件名就行”
  - 当前协议改为默认不重复导出，依赖链路判断，避免资源污染。

- [过时] “导出后只看目标文件是否存在”
  - 现在必须看源 GUID -> 目标路径链路，且目标存在，才算有效导出。

- [过时] “导出窗口只需要复制文件”
  - 商业级资源治理必须包含导出前依赖通报、冲突/重复处理、导出会话、回退、链路维护。

## 已完成内容

- [完成] 独立资产包分离窗口，中文友好，接入 `ESMenuTreeWindowAB`。
- [完成] 每个烘焙配置为 `ESAssetPackageBakeData : ESSO`。
- [完成] 基础资源分类：Prefab、Scene、Material、Texture、Model、Audio、Animation、SO、Shader、Font、Video、Other。
- [完成] 动作资源细分分类：待机&等待、步行、慢跑&奔跑、开始、停止、结束、转向、弧线、冲刺、Boost、蓄力、闪避、翻滚、滑行、跳跃、落地、墙面、体操、攻击、受击、死亡、武器、出生、其他。
- [完成] 排序：名称、路径、类型、大小、使用状态、动作分类、动作时长。
- [完成] 小格子动画帧缓存、磁盘持久化、视角区分、当前页优先队列。
- [完成] 大预览窗口实时渲染，不走缓存帧。
- [完成] 预览数据安全入口集中化：页面不应各自直接维护 ReloadDomain 清理、缓存刷新、模型/Avatar/材质解析规则。
- [完成] 导出前依赖通报。
- [完成] 导出复制加 `ES选用_` 前缀。
- [完成] 窗口内可直接配置“导出文件名前缀”，不用只依赖 SO Inspector。
- [完成] 默认禁止重复导出，除非配置开启覆盖已有链路目标。
- [完成] 导出会话、导出链路、Odin 字典链路、回退同步。

## 未完成/风险点

- [半完成] 导出配置入口仍偏 SO Inspector，窗口内集中配置体验不足。
- [半完成] 依赖通报目前是文本弹窗，后续应做可展开依赖树。
- [半完成] 链路维护工具不足：需要清理失效链路、Ping 源/目标、修复链路、批量解除链路。
- [半完成] 动画分类靠名称近似匹配，命名不规范会误分；后续需要手动覆盖分类或规则表。
- [风险] Unity 编辑器 Humanoid 采样仍有边界情况，尤其 Avatar 不匹配、Clip 仅 Root 曲线、模型材质/渲染管线异常。
- [风险] 不要重新引入临时 AnimatorController 资产。
- [风险] 不要把小格子缓存放入 `Assets/`，否则会造成导入污染和项目体积膨胀。
- [风险] ReloadDomain/编译后窗口恢复重绘时，不允许立刻批量解码小格子磁盘帧。PNG 文件磁盘很小，但 `Texture2D.LoadImage` 后会膨胀为原始纹理内存；大量 Entry 同时加载可能造成 Unity Editor 10GB~20GB 级内存占用。
- [当前防护] `ESAssetPackageGridAnimationFrameCache` 已限制 `MaxEntries=48`，默认同时小格子为 24，并在 ReloadDomain 后短时间暂停加载持久帧缓存；窗口会显示保护状态。不要恢复旧的 512 Entry/512 同时播放设计。
- [当前防护] ReloadDomain/退出前通过 `ESAssetPackagePreviewUtility.ClearPreviewCache()` 聚合清理，小格子帧缓存也在这条清理链里，不要绕开它单独管理预览纹理。
- [协作约束] 新增预览功能时，优先扩展 `ESAssetPackagePreviewWorkflow` 或其下游专职类；不要在 Home/Category/RecordPreview 页面里直接新增一套 EditorPrefs、缓存字典、ReloadDomain 回调或 StateMachineConfig 解析顺序。

## 商业级判断

具备商业级潜力，核心闭环已经成立：烘焙、筛选、预览、选择、依赖通报、导出、链路追踪、回退。

离商业级完成态还差：

- 导出配置面板产品化。
- 依赖树/冲突树可视化。
- 链路维护工具。
- 批量规则模板。
- 导出 dry-run。
- 自动化验证与导出审计日志。
