# 临时归档说明

2026-08-27 已将无项目引用的 MCP 截图 `arena-moba-lobby-mcp.png` 及其 `.meta` 迁移到 `ES/UIEvidence/reference-ingest/`。

正式 UI 资产仍放在 `Assets/UI`；测试截图和验证截图仍分别归属其现有测试/证据目录。本目录不再作为 AI MCP 截图默认落点；新的私有 AI 截图必须写入
`ES/AISpace/Local/Screenshots/<YYYYMMDD>/<agent-or-task>/`。
