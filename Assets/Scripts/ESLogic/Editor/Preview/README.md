# ES Editor Preview

这里放编辑器专用的预览辅助脚本。

它们不是 `Developer` 的内容，因为 `Developer` 只收框架使用者经常直接接触的 runtime 入口、轻封装和常用组件。

当前内容：

```text
EditorRememberedEntityTarget
ESEditorPreviewResourceScope
```

这些类只服务编辑器预览、资源清理、目标记忆和调试，不进入正式运行时主干。
