namespace ES.Commercial
{
    // Reserved namespace for optional commercial integrations. These integrations must use
    // their own domain data and never extend StateMachine with dynamic string parameters.
    internal static class CommercialEnhancements
    {
    }
}
