namespace ES
{
    /// <summary>
    /// 为 Unity 持久化提供与 ScriptableObject 类型同名的 MonoScript 身份。
    /// 数据与行为继续保留在 ESWorldMapData.cs 的同一 partial 类型中。
    /// </summary>
    public sealed partial class ESWorldMapAsset
    {
    }
}
