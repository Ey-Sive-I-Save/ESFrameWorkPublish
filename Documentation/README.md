# ES 框架 — 文档首页（面向 ES 用户）

欢迎使用 ES 框架。此处为文档入口页，提供本仓库文档的索引与导航。

建议的子目录（面向 ES 用户）：

- `API/`：查 API（类、方法、示例调用）。优先用于快速查找用法。
- `Design/`：阅读架构要点与模块约定（面向维护者与高级用户）。
- `Examples/`：示例代码与可运行片段，便于复制/测试。
- `Migration/`：升级与兼容说明，按版本列出迁移步骤。
- `RFC/`：提案与设计讨论记录，便于追踪决策历史。

文档维护建议：
- 使用 Markdown，标题和摘要要清晰；示例中标注对应源码路径。

