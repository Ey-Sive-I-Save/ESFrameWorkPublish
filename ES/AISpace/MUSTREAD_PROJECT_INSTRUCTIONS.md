# MUSTREAD marker

Before creating, moving, or classifying AI-generated content in `ES/AISpace`, read the project-root `AGENTS.md` and this directory's `README.md`.

The canonical body is `ES/AISpace/README.md`; machine-readable identity is
`ES/AISpace/AISPACE_AUTHORITY.json`. This marker contains no duplicate placement rules.
