# npm 代码工具样本

来源 npm registry（<https://www.npmjs.com/>）。下载 10 个小型实用包（is-odd、is-even、is-number、is-string、is-arrayish、is-callable、is-regex、is-arguments、has-symbols、is-core-module）`.tgz`，总下载 83,653 bytes（≤10 MB）。使用 tar 解包到 `packages/`，共 85 个可见文件、248,714 bytes；压缩包 staging 已删除。逐包 SHA-256 见 `hashes.json`，无 ZIP/TGZ 残留。未运行 Unity/Hooks。
