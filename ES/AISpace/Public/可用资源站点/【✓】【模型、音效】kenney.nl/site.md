# Kenney

- 来源：kenney.nl/assets；许可证：CC0（官方资产页）
- 支持类型：模型、音效；可搜索：是；支持部分下载：是
- 预算：10 MiB；当前可见资源：模型 10、音效 52；压缩包：0

## 探索技巧（2026-08-30）

- 资产目录可按类型筛选，优先使用官方资产页的 ZIP 直链；下载后立即解压并删除 ZIP/staging。
- 音效资源以 `.ogg` 为主，模型以 `.glb` 为主；按扩展名和页面标签分类，不用文件名推断许可证。
- 探查：`https://kenney.nl/assets` 返回 HTTP 200；本轮未遇到登录、验证码或限流。

## 证据与边界

文件已落盘于本目录 `模型/`、`音效/`，实际哈希与大小见 `provenance.json`。仅证明公开下载和本地可见，不代表 Unity/URP 导入或运行时验收。
