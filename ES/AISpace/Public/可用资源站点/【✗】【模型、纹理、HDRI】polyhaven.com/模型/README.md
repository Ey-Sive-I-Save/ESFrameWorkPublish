# Poly Haven 1K FBX 模型样本

依据 `ES/Output/ResourceCandidates/PolyHaven-纹理HDRI/polyhaven-models-hdri-candidates-temp.json` 的官方 API 直链下载 10 个 1K FBX 模型，总量 5,221,432 bytes（≤10 MB）。已临时打包并解包到 `models/`，随后删除 ZIP/staging；逐文件 SHA-256 见 `hashes.json`，无压缩包残留。资产 CC0；未运行 Unity/Hooks。
