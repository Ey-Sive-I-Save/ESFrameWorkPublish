# OpenGameArt

- 来源：opengameart.org；许可证：逐项核验（社区上传，不能按站点整体推断）
- 支持类型：模型、音效；可搜索：是；支持部分下载：是
- 状态：不可用（deferred-by-budget）；预算：10 MiB；当前可见资源：模型 39、音效 75；合计已超预算；压缩包：0

## 探索技巧（2026-08-30）

- 站内内容页可按标签和文件格式筛选；优先选择页面明确给出下载文件和许可证的条目。
- 本地模型包包含 `.blend/.fbx/.obj`，关联 PNG 作为模型依赖保留在模型目录，不单独宣称纹理类型。
- 探查：`https://opengameart.org` 返回 HTTP 200；未登录可访问首页，具体条目可能要求接受作者许可证；遇到无许可证条目须标记 unverified。

## 证据与边界

资源已解压到本目录 `模型/`、`音效/`，压缩包已清理。`provenance.json` 记录来源页、哈希和实际字节数；未验证 Unity/URP 导入。
