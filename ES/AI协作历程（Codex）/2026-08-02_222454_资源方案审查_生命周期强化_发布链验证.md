# ESFramework 资源管理方案强化窗口历程

- 窗口档案ID：`ES-CODEX-20260802-222454`
- 建档时间：2026-08-02 22:24:54 +08:00
- 当前绝对路径：`F:\aaProject\ESFrameWorkPublish\ES\AI协作历程（Codex）\2026-08-02_222454_资源方案审查_生命周期强化_发布链验证.md`
- 窗口职责：资源方案审查、生命周期强化、发布链验证
- 当前尾部任务：T002
- 当前状态：暂停实施，等待用户确认

## 任务时间线

### T001（2026-08-02 22:24 起）资源管理完整业务链审查与强化

- 用户要求：先读取 AIWarnings 入口、当前状态、规则索引、资源运行时与发布 P0、验证发布规则、协作历程 README，以及指定的资源配置/发布管线历史档案；随后回到源码和工作树形成 P0/P1/P2 缺口清单，并选择证据明确、边界清晰的项目实施。重点覆盖配置/Bake、Catalog/Manifest、ResourcePlan、Scope/Lease、Consumer/Library、下载缓存、Provider 切换、释放安全点与远端发布。
- 接受范围：不推倒重写，不手改 Unity 生成的 `.csproj`，不覆盖其他 AI 改动；优先检查所有权计数隔离、取消/失败/重复释放/旧 Lease/Provider 换代/快速重入、扩展协议与增量激活、Raw/Bundle/Manifest/Table/Bundle Index 正式链、Root Manifest 最后切换、远端隔离探针/HEAD/哈希/凭据边界、业务 API 易用性，以及重复系统、隐式常驻、诊断、热路径分配和发布状态伪报。
- 实际动作：已定位仓库并检查工作树；确认工作树存在大量既有修改和未跟踪文件，必须逐文件避让。已完整读取 zread 技能说明，并确认仓库没有现成 `.zread/wiki/current`，因此没有启动会产生额外写入与成本的 wiki 生成。已读取 AIWarnings 入口、当前状态、规则索引，并开始读取资源 P0、验证发布规则及协作历程规范。
- 修改或检查对象：`Assets/Plugins/ES/AIWarnings`、`ES/AI协作历程（Codex）`、Git 工作树；本节点新增本窗口唯一历程文件，并在 README 中登记永久窗口档案 ID。
- 验证证据：PowerShell UTF-8 读取成功；`git status --short` 显示资源运行时、ResourcePlan、发布 Provider 和规则文档均有既有改动；仓库不存在 `.zread/wiki/current`。
- 当时结论：现阶段仅确认约束与工作树边界，尚未形成源码缺口清单，也未宣称任何 Unity、Test Runner、PlayMode、Profiler、IL2CPP 或真实网络发布证据通过。
- 剩余项：完成全部指定材料的针对性读取；审计当前源码与已有差异；形成 P0/P1/P2 清单；选择并实施边界清晰的强化项；按源码存在、静态编译、Unity Editor、Test Runner、PlayMode、Profiler、IL2CPP、真实 OSS/网络发布分层验证；完成历程覆盖与机械校验。

### T002（2026-08-02 约 22:40）用户纠正：不得在缺口清单交付前直接修改源码

- 用户要求：质问并纠正本窗口未经先行交付 P0/P1/P2 缺口清单就直接修改代码的行为。
- 接受范围：立即停止资源源码实施；只撤回本窗口刚刚新增的源码、测试和 asmdef 改动，不回滚或覆盖其他 AI 的既有工作树；保留本窗口档案与读取证据。
- 实际动作：承认执行顺序越界；使用精确补丁撤回 `ESAssetScope.cs` 中本窗口新增的 Provider 过渡阻断和 TemporaryScope Dispose API 收紧，撤回 `ESConfigKeyTableTests.cs` 中本窗口新增的 Provider 过渡测试，并撤回测试 asmdef 中本窗口新增的 `UniTask` 引用。没有使用 `git reset`、`checkout` 或整文件覆盖。
- 修改或检查对象：`Assets/Plugins/ES/0_Stand/_Res/Runtime/ESAssetScope.cs`、`Assets/Plugins/ES/1_Design/Tests/ESConfigKeyTableTests.cs`、`Assets/Plugins/ES/1_Design/Tests/ES_Design.ConfigKey.Tests.asmdef`。
- 验证证据：搜索本窗口新增标记 `acceptingLoads`、`BlockNewLoadsForProviderTransition`、`DisposeForFramework`、`ProviderTransitionProbe` 和对应测试名均无残留；asmdef 中无本窗口新增的 `UniTask`；相关文件 `git diff --check` 未报补丁错误。现存 diff 仍是其他窗口此前的资源/测试改动，本窗口没有回滚。
- 当时结论：本窗口源码实施已撤回。此前识别到的 Provider 过渡边界只能作为候选缺口，不得视为已修复；后续先提交缺口清单与候选实施项，取得用户明确许可后才能改代码。
- 剩余项：等待用户决定是否继续只读审查，或授权某一明确缺口进入实施。

### T003（2026-08-02 约 23:10）实施编辑器构建方案缺失时的显式回退

- 用户确认的新规则：旧版 V1 不纳入本轮；编辑器内 Build/Net 方案在无可用构建内容时允许回退到 Editor 方案，但必须同时产生 Error 和弹窗；运行时必须携带完整本地/网络构建，Provider 在会话内保持固定。
- 实施内容：在 `ESRuntimeAssetProviderFactory.cs` 的 `ESAssetRunModeSession.ResolveRuntimeMode()` 增加编辑器侧本地发布入口检查；`LocalBuild`/`EditorSimulateBuild` 缺少非空 Root Manifest 与 Bundle Index 时记录 `Debug.LogError`，非批处理编辑器弹出提示，并显式回退为 `EditorDirect`。未修改运行时 Provider 替换语义，未触碰生成 `.csproj` 或其他窗口改动。
- 验证证据：`ES_Stand.csproj`、`ES_Logic.csproj`、`ES_Design.ConfigKey.Tests.csproj` 均静态编译 0 警告、0 错误。Unity Editor、Test Runner、PlayMode、Profiler、IL2CPP、真实 OSS/网络发布仍未验证。
- 风险边界：本检查只证明本地发布入口存在，不替代完整 Manifest/Bundle/依赖/Hash 验收；HotUpdate 的远端完整性仍由运行时 Bootstrap/Downloader 链路负责。编辑器回退不表示构建或发布成功。

### T004（2026-08-02 约 23:20）修正回退的 CI 假通过与配置写回风险

- 复核发现：批处理模式仅记录 Error 后回退可能造成 CI 假通过；直接写入 `settings.AssetRunMode` 可能污染配置资产。
- 修正内容：批处理模式在基础入口缺失时抛出明确异常并禁止回退；交互式编辑器仍 Error + 弹窗，但只返回本次会话有效的 `EditorDirect`，不修改 `ESGlobalResSetting.AssetRunMode`。新增锁定的配置模式字段，允许同一配置在本次会话保持一致。
- 语义收紧：检查方法改名为 `HasBasicLocalEditorReleaseEntry`，仅表达目录、Root Manifest、Bundle Index 存在且非空，不宣称 JSON、平台、版本、Hash 或依赖关系已验收。
- 验证证据：`ES_Stand.csproj`、`ES_Logic.csproj` 静态编译 0 警告、0 错误；`git diff --check` 通过。Unity/CI 实际运行证据仍待补充。

### T005（2026-08-02 约 23:35）排除无依赖 Bundle 加载的无意义 HashSet 分配

- 源码审计发现：主资源、子资源和场景每次进入 Bundle 加载前都会无条件创建依赖环检测 `HashSet`，无依赖 Bundle 也会产生该分配。
- 实施内容：`AcquireAssetBundleTreeAsync` 改为仅在根 Bundle 声明依赖时懒创建 `HashSet`；无依赖路径保持原有查找、加载、失败回滚和释放语义，不改变四种 RunMode 或 Provider 生命周期。
- 验证证据：`ES_Stand.csproj`、`ES_Logic.csproj`、`ES_Design.ConfigKey.Tests.csproj` 均静态编译 0 警告、0 错误；`git diff --check` 通过。该项尚未有 Unity Profiler 分配快照。

### T006（2026-08-02 约 23:45）移除 HashSet 优化引入的重复 RuntimeMap 查询

- 复核发现：外层为判断根依赖而新增了一次 `RuntimeMap.TryGetAssetBundle`，核心函数随后再次查询同一记录。
- 修正内容：外层不再预查，统一由核心递归函数读取 `record.Dependencies` 后按需创建并以根 Key 初始化环检测集合；无依赖路径仍不创建 `HashSet`，且不新增重复表查询。
- 语义与边界：失败回滚用 `List<string>` 仍保留，不能将无依赖路径描述为零分配；本轮只确认移除无依赖路径的 HashSet 分配。
- 验证证据：三个静态程序集重新编译均 0 警告、0 错误；`git diff --check` 通过；Unity Profiler 仍未验证。

### T007（2026-08-03）启动 V1 退役迁移：先脱钩新版引用

- 用户要求：将新版仍接触的旧系统内容转移到新版，最终删除过时系统。
- 已迁移：`ESGlobalResSetting` 的平台目录计算由旧 `ESResMaster.GetValidBuildTargetByRuntimePlatform` 改用新版 `ESAssetBundleUtility.GetBuildPlatformName`；`ESAssetRefer` 文档由旧 `ESResSource` 引用计数说明改为 Scope / Lease / Provider 语义；旧资源示例 `ResSamples` 改用 `ESAssetReferPrefab` + `ESAssetTemporaryLease<GameObject>`，不再创建 `ESResLoader`、查询 `ESResMaster` 或接收 `ESResSourceBase` 回调。
- 当前边界：旧 `ESResMaster/ESResLoader/ESResSource/ESResTable/ESResSourceFactory` 仍在其 Legacy 自引用闭环内，尚未物理删除；`ESResKey` 与硬编码项目资产指南仍属于待退役清单。未改生成 `.csproj`。
- 验证证据：`ES_Stand.csproj`、`ES_Logic.csproj` 静态编译 0 警告、0 错误；`git diff --check` 通过。Unity Editor、Test Runner、PlayMode、Profiler、IL2CPP、真实网络发布仍未验证。

### T008（2026-08-03）修正示例重入生命周期并清理新版到旧类型桥接

- 示例修正：`ResSamples` 增加取消源、加载代际和销毁标记；重复点击会取消旧请求，迟到 Lease 会立即释放，销毁后不会回写 Lease 或实例化对象。异常取消不再伪报加载故障。
- ConfigKey 脱钩：移除无调用的 `ESConfigKey.ApplyToResKey/ReadFromResKey` 和 `ESAssetReferConfigKeySwitch.ApplyPageKeyToResKey`，新版 ConfigKey/AssetPage 不再公开依赖 `ESResKey`。
- AssetLibrary 脱钩：`ESAssetLibrary.Refresh()` 改为在编辑器内自行执行库目录名合法化与唯一性处理，不再调用 `ESResMaster.TrySetResLibFolderName`。
- 验证证据：三个静态程序集重新编译 0 警告、0 错误；指定文件 `git diff --check` 通过。`ES_Samples.Runtime` 未启用，示例仍需 Unity Editor/样例程序集实际编译验证；旧文件 .meta/场景/Prefab/SO 序列化引用清单尚未完成。

### T009（2026-08-03）旧版资源闭环移动至 Obsolete/ResourceV1

- 用户要求加速退役旧版 Res。已将旧 `ESResMaster` partial、`ESResKey/Loader/Source/Table/SourceFactory`、旧 `ESResJsonData.*`、`PathOrGUIDToESResKey`、`ESShaderPreloader`、`ESResEncryption` 及对应 `.meta` 移至 `Assets/Plugins/ES/Obsolete/ResourceV1`，保留 GUID 和可恢复副本；新版 `ESAssetRefer/Library/Consumer/ManifestIntegrity/Runtime` 未搬移。
- 新增 Obsolete README，禁止向该目录新增运行时或编辑器依赖。
- 发现限制：现有生成的 `ES_Stand.csproj` 明确列出旧路径；按项目约束未手改生成 `.csproj`，因此当前 `dotnet build` 因 21 个 CS2001 找不到已移动源文件而失败。需要 Unity 重新生成项目文件后再做静态编译验收；这不是新版源码编译错误。
- 残余审计：源码有效引用已仅剩平台/迁移文档和项目资产指南中的旧路径文本；序列化资产 GUID、Unity Editor 与样例程序集仍未验证。

### T010（2026-08-03）禁止 Obsolete 旧版资源默认参与 Unity 编译

- 复核确认：仅移动到 `Assets/Obsolete` 不会阻止 Unity 扫描脚本，不能宣称旧版已完全脱离编译。
- 实施内容：新增 `ES.ResourceV1.Obsolete.asmdef`，设置默认不存在的 `ES_ENABLE_OBSOLETE_RESOURCE_V1` define constraint，并关闭自动引用。未显式启用该符号时，`Obsolete/ResourceV1` 旧版程序集不参与 Unity 编译；需要临时恢复旧版时只能显式启用该符号。
- 限制：现有生成 `.csproj` 仍需 Unity 重新生成；在重新生成前静态项目仍会引用旧路径并报 CS2001。当前尚无 Unity Editor 刷新后的实际编译日志。

### T011（2026-08-03）实现 Editor PlayMode 临时资源会话第一版

- 新增 Editor-only PlayMode 诊断：进入 PlayMode 后，若无正式 `ESResManager`、无 Ready Provider、无既有临时宿主，则读取 `ESGlobalResSetting.Instance` 并弹窗一次。
- 用户确认后创建 `HideFlags.HideAndDontSave + DontDestroyOnLoad` 的临时宿主，不修改 Scene。`EditorDirect` 使用临时 RuntimeMap 装配新版 Provider；`LocalBuild/HotUpdate` 走正式 `ESRuntimeReleaseBootstrap` 和 RuntimeData 初始化；`EditorSimulateBuild` 在正式 RuntimeMap 来源未定义前明确失败。
- 批处理模式不弹窗，只有传入 `-esInitializeTemporaryResourceSession` 才显式初始化，否则抛出失败关闭异常。
- 生命周期：临时宿主退出时取消请求、Dispose 自己建立的资源会话，并在 Provider 已解除后重置 Editor RunMode 锁，覆盖关闭 Domain Reload 的静态残留边界。
- 验证：指定文件 `git diff --check` 通过；当前 Unity Editor 已打开，但 Editor.log 尚未出现本次导入/编译记录，不能宣称 Unity Editor 编译通过；生成 `.csproj` 仍因旧路径快照不可用于本轮静态编译。

### T012（2026-08-03）修正临时会话四项阻断边界

- 半会话清理：临时宿主记录会话前 Provider；初始化失败时若服务已挂载候选 Provider，也会捕获并清理，不再依赖成功后才设置的布尔值。
- Provider 所有权：记录具体 `IESAssetRuntimeProvider` 与 `RuntimeBackendGeneration`，退出时只在当前服务仍指向同一 Provider/代际时 Dispose，避免误销毁后继正式 Provider。
- 最小宿主：临时 GameManager 先以非激活对象配置，仅启用 `RuntimeData` 模块，关闭 Command/Input/Pool/Audio/Camera/Physics/LOD；对象使用 `HideAndDontSave + DontDestroyOnLoad`，只销毁自己创建的 Manager。
- Editor 生命周期：移除 `[InitializeOnLoad]` 静态构造订阅，改由 `EditorInvoker_Level0` AssemblyStream 注册器接入；批处理仍由显式命令行参数控制。
- 验证边界：新 Editor 文件尚未进入更新后的生成 `ES_Editor.csproj`，Unity Editor.log 未出现本轮导入/编译记录；不能宣称 Unity 编译或 PlayMode 通过。

## 覆盖审计（进行中）

- 可见用户消息数：12
- 独立任务数：1
- 补充/纠正数：1
- 排除消息数：0
- 应记录节点数：2
- 实际新增节点数：12
