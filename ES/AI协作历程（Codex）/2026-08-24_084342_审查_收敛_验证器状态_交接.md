# ES 协作治理收敛、AIBrain 入口审查与验证器状态交接

文件名大纲：审查协作治理_收敛AIBrain入口_强化验证器状态_准备交接

窗口档案ID：`ES-CODEX-20260824-084342`

会话范围：本窗口围绕 ES Agent 门户、AIBrain、Skill 组合验收、静态/Runtime 语义和交接准备展开。

## 交接结论

ES 已具备商业级 Agent 协作架构的核心骨架，但全量交付证据尚未闭合。当前应将结论限定为：治理入口和发现入口已强化；Skill Portfolio、部分静态边界、Evidence Receipt 与 Unity/Runtime 验收仍未全部完成。

## 任务时间线

### T001：复核 AGENTS.md 是否真实强化

- **用户要求**：确认根 `AGENTS.md` 是否真正强化，而不是只有文案。
- **实际动作**：检查严格 UTF-8、行数、字节数、硬编码盘符、差异格式和引用入口。
- **证据**：当前文件约 51 行、约 7.2 KiB；严格 UTF-8 通过；无盘符硬编码；仍是工作区未提交修改。
- **结论**：AGENTS 已成为稳定治理入口，负责授权边界、状态分域、AIBrain/Knowledge 发现链和 Static/Runtime/Release 分离；它不是全量验收通过证明。

### T002：解释 Portfolio 的大量阻断

- **用户要求**：解释为什么出现大量 Semantic / Boundary / Evidence 阻断。
- **实际动作**：读取 `ES/Output/SkillPortfolioReceipt.json` 和 `SkillPortfolioInnerValidation.json`，按 Profile 统计。
- **证据**：此前组合扫描为 47 个 Skill × 多个 Profile；旧式汇总显示约 135 条 blocked，主要来自 Semantic、Boundary 和 Evidence，不是 135 个独立代码 Bug。
- **结论**：Semantic 多为 authority/Knowledge/AIBrain 绑定缺口；Boundary 多为命令绑定、动态路径或静态无法证明；Evidence 多为缺失、过期或未绑定案例回执。

### T003：拆分验证状态语义

- **用户要求**：不能把缺证据误认为代码失败。
- **实际修改**：在 `.agents/skills/es-skill-validator/scripts/Invoke-ESSkillValidation.ps1` 增加 `claimStatus`，保留旧 `status` 兼容字段。
- **新状态**：`StaticDefect`、`EvidenceMissing`、`EvidenceStale`、`EvidenceUnbound`、`ManualReviewRequired`、`RuntimeNotRun`、`Passed`，并允许 `NotApplicable`。
- **验证**：单 Skill 验证已能同时输出旧状态和新语义，例如 Semantic=Passed、Boundary=ManualReviewRequired、Evidence=EvidenceUnbound。

### T004：按职责收敛 Evidence 案例

- **用户要求**：不再为所有 Skill 强制五个案例，避免过度设计。
- **实际修改**：Evidence 预期案例改为读取 `static-replay.manifest.json.responsibilityProfile`，按 governance、knowledge、engineering、authoring、session、release 等职责选择最小有效集合。
- **规则**：只读 engineering 只需正向/非法输入；knowledge 增加 Hash 变化；写入/authoring 才要求拒绝扩权或幂等；session 要求中断恢复；release 才使用完整集合。
- **保护**：旧 `governance.json.requiredCases` 作为允许上限，不再盲目作为所有 Skill 的硬门禁；不生成没有决策价值的假回执。

### T005：Portfolio 增加分层统计

- **实际修改**：`Test-ESSkillPortfolio.ps1` 增加 `staticReadyCount`、`evidencePendingCount`、`runtimeRequiredCount`、`runtimeNotRunCount`。
- **最近一次报告**：50 个 Skill；`staticReadyCount=2`；`evidencePendingCount=46`；`runtimeRequiredCount=23`；Portfolio 仍为 `blocked`。最后一项在下一次重跑时应按 Runtime-required 计入未运行数量。
- **边界**：组合报告旧 `validatorBlocked` 仍为兼容汇总，不应单独用于判断具体失败原因。

### T006：收敛 AIBRAIN_ENTRY.md

- **用户要求**：AIBrain 入口不要变成冗余总规范。
- **实际修改**：删除重复的 Knowledge 质量门禁、权限、证据和 Runtime 协议展开，改为最小规则和权威文件指针；保留启动链、RouteKeys、功能区表、SourceRef/Hash/stale 最小语义和 Skill 刷新路由。
- **结果**：文件由约 148 行/18.3 KiB 收敛到约 117 行/14.7 KiB；路由表保留，AIBrain 仍能找到 Knowledge、Skill、AIWarnings、AICommand 和 Automation 入口。
- **验证**：严格 UTF-8 通过；`git diff --check` 通过；AIBRAIN_ENTRY 哈希已重新生成到 Registry Manifest。

### T007：清理 Skill 合同重复

- **实际修改**：合并 `es-skill-governance/SKILL.md` 和 `es-skill-validator/SKILL.md` 中重复的 `Specialized static acceptance` 段落。
- **结果**：每个文件现在只有一份 Specialized acceptance 和一份 Responsibility-specific acceptance；没有删除验收规则。

### T008：建立全量中文 Skill 自发现职责

- **用户要求**：为所有 Skill 增加真实中文路由，确保 AI 能自主发现能力，而不是要求用户知道英文 Skill 名称。
- **工作职责**：负责中文自然语言路由注册、唯一命中解析、歧义/无命中处理和全量覆盖静态验证；不负责授予执行权限或启动 Runtime。
- **实际修改**：新增 `.agents/SKILL_ROUTE_ALIASES.zh-CN.json`，覆盖全部 51 个直接 Skill；新增 `.agents/skills/es-skill-governance/scripts/Resolve-ESChineseSkillRoute.ps1`；在 AIBRAIN_ENTRY、AGENTS、`.agents/README.md` 和 Skill Governance 入口登记发现规则。
- **路由原则**：唯一命中后读取项目内对应 `SKILL.md` 与 `governance.json`；多命中按对象/动作/风险消歧；无命中返回 `NoSkillRoute` 并回到 AIBrain/KnowledgeIndex，不猜测、不全量扫描；中文别名不授予权限。
- **静态验证**：`Test-ESChineseSkillRouteCoverage.ps1` 结果为 `passed`，51/51 覆盖，缺失 0、孤儿 0、歧义 0。典型交接、创建 Skill、编辑器布局、业务分析目标均唯一命中预期 Skill。
- **未证明**：尚未进行 AIBrain/Unity 内真实路由回放；当前结论是注册表、解析器和项目入口静态闭合，不等同于 Runtime 自动注入已通过。

## 当前权威入口

- 治理根：[AGENTS.md](../../AGENTS.md)
- AIBrain 发现：[AIBRAIN_ENTRY.md](../../Documentation/AIKnowledge/AIBRAIN_ENTRY.md)
- Knowledge 路由：[KnowledgeIndex.yaml](../../Documentation/AIKnowledge/KnowledgeIndex.yaml)
- Skill 组合门禁：`.agents/skills/es-skill-validator/scripts/Test-ESSkillPortfolio.ps1`
- Skill 架构门禁：`.agents/skills/es-skill-governance/scripts/Test-ESSkillArchitecture.ps1`
- 商业一致性：`.agents/skills/es-skill-governance/scripts/Test-ESCommercialCoherence.ps1`

## 尚未完成与禁止误读

- Portfolio 当前仍为 `blocked`；不能称 50 个 Skill 全部交付。
- Architecture 报告仍可能因 AICommand/Skill Registry Hash 漂移而 blocked，需要稳定同一源码快照后重建并复核。
- Evidence Receipt 仍未全量生成和绑定；不得为了降低数字伪造回执。
- 23 个 Runtime-required Skill 尚未取得 Runtime 证据；没有启动 Unity、PlayMode、Profiler、Player、IL2CPP、网络或发布流程。
- 静态通过只能证明源码、合同、路径、路由、哈希和确定性回放范围，不能证明真实视觉、性能或运行行为。
- 本窗口没有执行 Git 暂存、提交、推送、删除、重命名或发布。

## 下一窗口建议

1. 先重跑 `Test-ESSkillPortfolio.ps1`，确认新的 claimStatus 和四项 Portfolio 计数已写入最新报告。
2. 按 `StaticDefect`、`ManualReviewRequired`、`EvidenceMissing/Stale/Unbound` 分组处理，不再按旧的 blocked 总数批量修复。
3. 先修 Semantic authority/Knowledge 绑定，再处理真实 Boundary 缺陷和受控例外。
4. 只为职责真正需要的案例生成静态 Receipt；`not-applicable` 不生成假材料。
5. 所有 Runtime/Release 动作必须在新任务中获得明确授权后单独执行。

## 当前工作职责

本窗口负责：ES Agent 协作治理收敛、AIBrain 发现入口减法、Skill 验证状态分层、Evidence 案例按职责收敛、Skill 中文路由注册与静态自发现解析。接续 AI 应优先读取本档案、AGENTS.md、AIBRAIN_ENTRY.md 和中文路由注册表，再按目标命中 Skill。

## 交接状态

`handover-ready-for-static-continuation`：可从静态治理和证据分层继续；不可将当前状态解释为全量商业交付或 Runtime 已验收。
