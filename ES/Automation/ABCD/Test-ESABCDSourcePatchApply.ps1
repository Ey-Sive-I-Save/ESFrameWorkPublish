[CmdletBinding()]param([string]$ProjectRoot=(Resolve-Path (Join-Path $PSScriptRoot '..\..\..')).Path,[string]$ReportPath='ES/Output/StaticReplay/es-abcd-source-patch-apply.json')
$ErrorActionPreference='Stop';$root=(Resolve-Path $ProjectRoot).Path;Import-Module (Join-Path $root 'ES/Automation/ABCD/ESABCDIterationFeedback.psm1') -Force
$target='ES/Output/StaticReplay/iteration-patch-fixture.txt';$full=Join-Path $root $target;$before=(Get-FileHash $full -Algorithm SHA256).Hash.ToLowerInvariant();$current=(Get-Content -Raw -Encoding UTF8 $full).Trim();if($current-eq'ABCD_ITERATION_FIXTURE_V1'){$r=Apply-ESABCDSourcePatch -ProjectRoot $root -TargetPath $target -ExpectedHash $before -OldText 'ABCD_ITERATION_FIXTURE_V1' -NewText 'ABCD_ITERATION_FIXTURE_V2')}else{$r=[pscustomobject]@{status='already-applied';beforeHash=$before;afterHash=$before}};$after=(Get-Content -Raw -Encoding UTF8 $full).Trim();$cases=@([pscustomobject]@{case='exact-source-apply';status=if(($r.status-eq'applied' -or $r.status-eq'already-applied') -and $after-eq'ABCD_ITERATION_FIXTURE_V2'){'passed'}else{'failed'}},[pscustomobject]@{case='hash-precondition';status=if($r.afterHash-ne''){'passed'}else{'failed'}});$failed=@($cases|Where-Object {$_.status -eq 'failed'});$overall='passed';if($failed.Count){$overall='failed'};$refs=@('ES/Automation/ABCD/ESABCDIterationFeedback.psm1','ES/Automation/ABCD/Test-ESABCDSourcePatchApply.ps1','ES/Output/StaticReplay/iteration-patch-fixture.txt');$hashes=[ordered]@{};foreach($ref in $refs){$hashes[$ref]=(Get-FileHash (Join-Path $root $ref) -Algorithm SHA256).Hash.ToLowerInvariant()};$report=[ordered]@{schemaVersion=1;validator='Test-ESABCDSourcePatchApply';status=$overall;staticStatus=if($failed.Count){'static-failed'}else{'static-passed'};runtimeStatus='runtime-not-run';evidenceLevel='S1';capturedUtc=[DateTime]::UtcNow.ToString('o');caseCount=$cases.Count;passedCount=($cases.Count-$failed.Count);failedCount=$failed.Count;cases=$cases;authorizationKind='read-only';writeScope=@($target);sourceRefs=$refs;sourceRefHashes=$hashes;evidenceContractId='es.skill-evidence-receipt';evidenceContractHash=(Get-FileHash (Join-Path $root 'ES/Automation/Contracts/es-skill-evidence-receipt-v1.schema.json') -Algorithm SHA256).Hash.ToLowerInvariant();skillName='es-agent-mechanism-replication';case='abcd-source-patch-apply';toolId='es-abcd-source-patch-apply-validator';receiptPath=$ReportPath.Replace('\','/');unityVersion='not-run';claimsNotProven=@('Unity compilation')};$out=Join-Path $root $ReportPath;New-Item -ItemType Directory -Force (Split-Path $out)|Out-Null;[IO.File]::WriteAllText($out,($report|ConvertTo-Json -Depth 20),[Text.UTF8Encoding]::new($false));$report|ConvertTo-Json -Depth 20;if($failed.Count){exit 1}
