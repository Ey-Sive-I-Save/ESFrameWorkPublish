# Skill 最小质量矩阵

| 维度 | 至少覆盖 |
|---|---|
| 触发 | 一个应触发任务、一个相邻但不应触发任务 |
| 读取 | Knowledge routeKeys、RequiredReads、关联 Skill |
| 权限 | 正常范围、越权请求、候选目录限制 |
| 失败 | 缺失依赖、工具不可用、外部超时或取消 |
| 证据 | 实际 diff、结构验证、未运行项和 stale 风险 |
| 迭代 | 失败样本、最小修订、修订后复测 |

不得把“文件存在”当成 Skill 已通过；不得把静态检查当成 Unity、Player 或外部服务验收。
