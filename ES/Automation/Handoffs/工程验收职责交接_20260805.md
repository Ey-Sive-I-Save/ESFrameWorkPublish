# ES 工程验收升级职责交接

状态：交接草案，供下一 Codex/验收 AI 接手前复核；不等于源码、Unity 或发布验收通过。

## 交接目的

当前 AI 的主要职责已接近结束，需要把“工程验收升级、证据分层、Codex 新窗口启动与交接治理”交给新的 AI 窗口继续维护。交接只传递导航、事实、证据和边界，不自动转移源码修改、Git、Unity、发布或删除权限。

## 当前窗口身份

- 项目：`F:\aaProject\ESFrameWorkPublish`
- 分支：`main`
- 本次核对 HEAD：`bc4d755e248b0e6106e2c9313bc559da88a70c28`
- 当前工作树：大量已有修改、未跟踪文件和并行交付；新 AI 必须先重新执行 `git status --short`，不得据此文件判断所有改动归属。
- 当前窗口 session：以本地 Codex history 精确核对为准；本交接不凭主题或最近时间自动认领旧档案。

## 本窗口实际完成的职责

### 1. 新 Codex 启动与任务交接治理

已核对并强化：

- `.agents/skills/es-codex-session-bootstrap/SKILL.md`
- `.agents/skills/es-codex-session-bootstrap/references/session-bootstrap-contract.md`
- `.agents/skills/es-codex-session-bootstrap/scripts/Start-ESCodexSession.ps1`
- `ES/AI协作历程（Codex）/Tools/Test-ESCodexTimelineCoverage.ps1`

当前启动约束：

- 固定项目根目录。
- 先 Validate/DryRun，再允许 New。
- 通过 npm `codex.cmd` 和可见 `cmd.exe` 启动。
- 中文交接上下文放在 `ES/Automation/Handoffs/CODEX_TASK_CONTEXT.md`，不把长中文方案塞进易失真的命令行。
- `TaskKey` 生成稳定任务指纹。
- 同一任务重复启动返回 `alreadyRunning`，不重复开窗。
- 只有显式 `-ForceNew` 才允许第二个独立窗口。
- 启动后记录进程 ID、任务指纹，并从本地 history 尝试登记 session ID。

### 2. 实际启动冒烟证据

已执行一次中文三位数乘法窗口冒烟：

- TaskKey：`launcher-smoke-test-3digit-multiplication-20260805`
- Process ID：`67108`
- Session ID：`019fce6e-ae00-7da2-b4cf-7bc57b168daa`
- 首次调用：`launched = true`
- 相同 TaskKey 第二次调用：`alreadyRunning = true`、`launched = false`

该证据只证明启动器的 CMD、任务指纹和去重路径工作；不证明新窗口业务回答内容或工程验收通过。

### 3. 通用任务上下文交接

当前通用交接上下文：

`ES/Automation/Handoffs/CODEX_TASK_CONTEXT.md`

该文件当前承载 AITest 方案本地化任务，但它是通用 Codex 任务上下文，不是 AITest 基础设施规范。

### 4. 历程完整性和交接规则

已强化：

- 阶段只能做容器，不能替代独立 `Txxx` 任务节点。
- 历程恢复必须执行 `Test-ESCodexTimelineCoverage.ps1`。
- 用户消息数、节点数、编号、字段和阶段结构不一致时禁止宣称完整恢复。
- 完整审计结束后才询问一次是否生成 AI 交接文案。
- 交接文案不扩大权限。
- 模块审计商业可行性标准已加入 `AI协作历程与模块审计_商业可行性验收标准.md`，当前尚未取得 C2/C3 全部证据。

## 当前并行/外部状态，禁止冒领

当前工作树中存在以下可能来自其他窗口或并行交付的内容：

- `Packages/com.esframework.aitest/`
- `Assets/Scripts/ESLogic/Runtime/Developer/AITest/`
- `Assets/Scripts/ESLogic/Editor/Developer/`
- Graph、ESAutomationCenter、AdvancedDialog、资源 Scope、AIWarnings、台账等大量修改。

本窗口没有对这些并行内容做完整实现归属确认。新 AI 必须按当前源码、Git diff、文件时间和 session 证据逐项复核，不能把目录存在写成“本窗口已实现”。

## 证据矩阵

| 层级 | 当前状态 | 说明 |
|---|---|---|
| 启动脚本源码 | 已实现 | CMD、TaskKey、去重、进程标记、session ID 尝试登记已写入脚本 |
| launcher PowerShell 语法 | 已通过 | 当前脚本解析无语法错误 |
| 严格 UTF-8/空白 | 已通过 | 相关 Skill、脚本、交接文档和 P0 规则已检查 |
| New 启动冒烟 | 已通过 | 三位数乘法测试窗口成功返回 processId 和 sessionId |
| 同任务去重 | 已通过 | 第二次同 TaskKey 返回 alreadyRunning |
| 新窗口业务输出 | 未核对 | 只确认窗口启动，不把业务回答写成已验证 |
| Unity Editor 编译 | 未验收 | 启动器与交接治理不等于 Unity 编译 |
| Unity Test Runner/PlayMode | 未验收 | 不由本次启动冒烟替代 |
| Player/IL2CPP/Profiler | 未验收 | 无实跑证据 |
| AITest Runtime/Package | 未签收 | 若工作树存在实现，必须按当前源码和 Unity 证据重新审计 |
| 商业级治理 C2/C3 | 未达成 | 尚缺长窗口恢复、失败注入、跨 AI 接手、并行权限和长期回归证据 |

## 下一 AI 的最小接手流程

1. 读取 AIWarnings `README`、`CurrentStatus`、`RuleIndex`。
2. 读取本文件、`CODEX_TASK_CONTEXT.md`、`es-codex-session-bootstrap` Skill 与 session bootstrap contract。
3. 运行 `git status --short`、`git rev-parse HEAD`，确认工作树漂移。
4. 使用 `Find-CodexSession.ps1` 精确核对需要接手的 session；搜索分数不授予归属。
5. 检查启动器状态标记和当前 `cmd/codex` 进程；不要因为旧窗口存在就自动 `ForceNew`。
6. 重新评估 AITest、AutomationCenter、Input、SceneValidation 和报告路径的当前真实状态。
7. 先给出中文计划和证据矩阵，再执行当前用户明确授权的最小动作。
8. Unity、Player、PlayMode、IL2CPP、Profiler 或发布结论必须有对应真实证据。

## 权限边界

- 本文件不授权源码实现、删除、迁移、Git 暂存/提交/推送、Unity 执行、远端上传或发布。
- `TaskKey` 只用于窗口去重，不是实现权限。
- 旧 session ID 只用于导航，不是当前窗口写入权限。
- AITest 交接上下文只描述方案和边界，不代表 AITest 已经可用。

## 交接后的最终询问

下一 AI 完成一次真正的完整审计或交接工作流后，才在最终答复末尾醒目询问用户是否需要生成新的可复制 AI 交接文案；本文件本身不能被当作“已完成全部工程验收”的证明。
