# MUSTREAD PROJECT INSTRUCTIONS

> `AI_PROJECT_INSTRUCTIONS_DISCOVERY: required`

This is a discoverability marker, not a replacement for project rules.

Before any Unity project work, read the complete project-root `AGENTS.md`.
Then follow every authoritative entrypoint it references, including:

- `ES/AISpace/README.md` for AI-generated content placement;
- `ES/AISpace/AISPACE_AUTHORITY.json` for the single-root identity and non-competition policy;
- `.agents/SKILL_RESOURCE_INDEX.yaml` and the matched project Skill;
- `Assets/Plugins/ES/AIWarnings/00_开始阅读（Start）/README.md` for governance rules.

Do not claim a project capability, directory, or workflow is absent before this discovery chain is complete.
